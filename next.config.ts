import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  allowedDevOrigins: [
    "neil-snapshot-aaa-cumulative.trycloudflare.com",
    "warned-figures-boundary-doll.trycloudflare.com",
    "pointed-designers-calling-allows.trycloudflare.com",
    "independence-obligation-tariff-precise.trycloudflare.com",
    "indie-cement-isolation-statements.trycloudflare.com",
  ],
};

export default nextConfig;
